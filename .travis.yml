sudo: required
services:
  - docker:

before_install:
  - docker build -t sushimonster/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run sushimonster/react-test npm test -- --coverage 

after_success:
  - docker build -t sushimonster/multi-client ./client
  - docker byuld -t sushimonster/multi-nginx ./nginx
  - docker build -t sushimonster/multi-server ./server
  - docker build -t sushimonster/multi-worker ./worker
  # Log into Docker CLI
  - echo "$DOCKER_PASSOWRD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take these images and push to Docker-Hub
  - docker push sushimonster/multi-client 
  - docker push sushimonster/multi-nginx
  - docker push sushimonster/multi-server
  - docker push sushimonster/multi-worker    